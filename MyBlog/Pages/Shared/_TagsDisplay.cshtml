@model IEnumerable<string>

<div class="tags text-accent text-truncate mt-4 mt-md-5 mb-1 d-flex">
    @foreach (var tag in Model)
    {
        <span class="badge badge-accent mx-1 font-weight-normal text-uppercase">@tag</span>
    }
</div>
